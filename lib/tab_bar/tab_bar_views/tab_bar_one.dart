import 'package:flutter/material.dart';

class TabBarOne extends StatelessWidget {
  const TabBarOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Tab Bar One !!!"),),
    );
  }
}
